{% if not cl_clubs is defined %}
	{% set cl_clubs = '' %}
{% endif %}
{% if not cl_equipes is defined %}
	{% set cl_equipes = '' %}
{% endif %}
{% if not cl_membres is defined %}
	{% set cl_membres = '' %}
{% endif %}

<nav class="navbar navbar-expand-lg navbar-light bg-light rounded">
	<a class="navbar-brand" href="#"><span class="oi oi-home"></span></a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
			
	<div class="collapse navbar-collapse" id="navbarTogglerDemo01">
		<ul class="navbar-nav mr-auto mt-2 mt-lg-0">
			<li class="nav-item {{ cl_clubs }}"><a href="{{ path('gt_admin_clubs') }}" class="nav-link">{{ 'gt.admin.tab.label.clubs'|trans() }} <span class="sr-only">(current)</span></a></li>
			<li class="nav-item {{ cl_equipes }}"><a href="#" class="nav-link">{{ 'gt.admin.tab.label.equipes'|trans() }}</a></li>
			<li class="nav-item {{ cl_membres }}"><a href="#" class="nav-link">{{ 'gt.admin.tab.label.membres'|trans() }}</a></li>
		</ul>
	</div>
</nav>
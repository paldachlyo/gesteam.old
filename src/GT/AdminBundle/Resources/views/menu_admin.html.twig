{% if not cl_clubs is defined %}
	{% set cl_clubs = '' %}
{% endif %}
{% if not cl_equipes is defined %}
	{% set cl_equipes = '' %}
{% endif %}
{% if not cl_membres is defined %}
	{% set cl_membres = '' %}
{% endif %}

<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="#"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a>
		</div>
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">
				<li class="{{ cl_clubs }}"><a href="{{ path('gt_admin_clubs') }}">{{ 'gt.admin.tab.label.clubs'|trans() }}</a></li>
				<li class="{{ cl_equipes }}"><a href="#">{{ 'gt.admin.tab.label.equipes'|trans() }}</a></li>
				<li class="{{ cl_membres }}"><a href="#">{{ 'gt.admin.tab.label.membres'|trans() }}</a></li>
			</ul>
		</div>
	</div>
</nav>